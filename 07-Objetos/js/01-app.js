const nombre = 'Monitor 30 "';
const precio = 499;
const disponible = true;

// Un objeto agrupa todo en una sola variable
// Object literal
const producto = {
    nombre: 'Monitor 30"',
    precio: 500,
    disponible: true,
};

console.log(producto);
console.log(producto.nombre);
